# 019 — sentry-crash-reporting · scratchpad

Promoted 2026-06-06. Durable learnings promoted to
[[023-bug-clerk-isloaded-boot-hang]] (the non-throwing boot-hang mechanism + the
white-splash storyboard bug + the unsigned-sim keychain confound),
[[024-bug-npm10-npm11-lockfile-divergence]] (EAS-red/CI-green lockfile split + the
npm@10 minimal-delta repair recipe), and
[[025-pattern-sentry-observability-wiring]] (SSR-safe init, watchdog milestones,
sensitive EAS token path, DSN hard-guard, no-env-gated-test-throw rule).

Run record: 6 panel calls (scope ×2 rounds, approach ×2 rounds, whole-proposal
×2 rounds), 0 honest skips, 0 panel escalations, 2 user overrides (completion
panel + promote partition skipped on explicit "merge the pr, then do cleanup"
instruction; residual risk — Sentry-side event arrival unconfirmed pre-merge —
recorded and covered by the post-merge protocol in docs/ios-release.md).

[synthesis] deferred (3 new entries 023–025 since watermark; offer minerva:synthesize
on a future pass)
